# 并发编程

## RAII
Resource Acquisition Is Initialization ：资源获取即初始化
管理资源的重要思想。例如：动态分配内存、文件描述符、网络套接字、锁
就是将资源申请、释放封装，实现在局部作用域内申请资源然后销毁资源

## 读写锁
`shared_mutex`
利用RAII思想，使用`shared_lock`和`unique_lock`进行读写锁的上锁与解锁
**注意在其他函数手动调用`shared_lock`的析构函数，不会改变是否持锁的状态**